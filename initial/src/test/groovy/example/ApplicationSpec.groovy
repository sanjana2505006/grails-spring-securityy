package example

import spock.lang.Specification

/**
 * Sanity check for the starter project before guide changes are applied.
 */
class ApplicationSpec extends Specification {

    void 'starter project loads'() {
        expect:
        true
    }
}
